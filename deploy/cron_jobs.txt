┌─────────────────────────────────────────────────────────────────────────┐
│                         Scheduled Jobs                                  │
└─────────────────────────────────────────────────────────────────────────┘

  8c71d7dddafb [active]
    Name:      Session maintenance
    Schedule:  every 360m
    Repeat:    ∞
    Deliver:   local
    Script:    session_maintenance.py
    Mode:      no-agent

  f1915c187f11 [active]
    Name:      L2 Ticket Scout
    Schedule:  every 2m
    Repeat:    ∞
    Deliver:   local
    Profile:   l2-investigator
    Script:    l2_pipeline_runtime.py
    Mode:      no-agent

Notes:
- `l2_pipeline_runtime.py` defaults to `scout` when invoked with no positional mode, which is how Hermes no-agent script jobs execute it.
- Scout is the only scheduled Chitragupta L2 lifecycle entrypoint. It reconciles existing work before attempting a new claim.
- Publication, rejection handling, normalization and orphan recovery are internal lifecycle operations, not separate jobs.
- Hermes owns session maintenance. GBrain owns its own sync/embedding/graph/dream/maintenance scheduling.
