┌─────────────────────────────────────────────────────────────────────────┐
│                         Scheduled Jobs                                  │
└─────────────────────────────────────────────────────────────────────────┘

  8c71d7dddafb [active]
    Name:      Session maintenance
    Schedule:  every 360m
    Repeat:    ∞
    Deliver:   local
    Script:    session_maintenance.py
    Mode:      no-agent (script stdout delivered directly)

  f1915c187f11 [active]
    Name:      L2 Ticket Scout
    Schedule:  every 2m
    Repeat:    ∞
    Deliver:   local
    Script:    ticket_scout.py
    Mode:      no-agent (script stdout delivered directly)

Notes:
- `ticket_scout.py` is the only scheduled Chitragupta L2 lifecycle entrypoint. It runs the
  deterministic lifecycle reconciler before attempting a new claim and then performs the
  best-effort learning/GBrain convergence cycle.
- Reviewer publication, rejection handling, repair and orphan recovery are all owned by
  `l2_pipeline_runtime.py`; there are no separate publisher/reject/audit lifecycle jobs.
- Hermes owns session maintenance and memory-provider plumbing. Chitragupta does not run
  a custom Mem0 patch/reapply/Qdrant job.
