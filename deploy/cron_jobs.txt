┌─────────────────────────────────────────────────────────────────────────┐
│                         Scheduled Jobs                                  │
└─────────────────────────────────────────────────────────────────────────┘

  8c71d7dddafb [active]
    Name:      Session maintenance
    Schedule:  every 360m
    Repeat:    ∞
    Deliver:   local
    Script:    session_maintenance.py
    Mode:      no-agent (script stdout delivered directly)

  f1915c187f11 [active]
    Name:      L2 Ticket Scout
    Schedule:  every 2m
    Repeat:    ∞
    Deliver:   local
    Script:    l2_pipeline_runtime.py scout
    Mode:      no-agent (script stdout delivered directly)

Notes:
- `l2_pipeline_runtime.py scout` is the only scheduled Chitragupta lifecycle entrypoint.
- Each scout reconciles existing work before any new claim and performs best-effort reviewed-outcome materialization.
- Reviewer publication, rejection handling, repair and orphan recovery are internal lifecycle steps, not separate jobs.
- Hermes owns session maintenance. GBrain owns its own sync/embedding/graph/dream/maintenance jobs.
